# lens

Tool dispatch layer. Runs on lens (Pi 5, NixOS).

Planned: receive tool calls from obscura, execute in sandbox (opencode / Claude Code),
return results. Async completion delivered to reMarkable via obscura.
